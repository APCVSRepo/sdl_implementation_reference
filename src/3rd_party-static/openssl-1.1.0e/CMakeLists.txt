set(OPENSSL_INCLUDE_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/inc32)
set(OPENSSL_INCLUDE_DIRECTORY ${OPENSSL_INCLUDE_DIRECTORY} PARENT_SCOPE)

set(TARGET openssl)

add_custom_target(${TARGET}
  COMMAND config
  COMMAND make
  WORKING_DIRECTORY ${CMAKE_CURRENT_BIN_DIR}
)